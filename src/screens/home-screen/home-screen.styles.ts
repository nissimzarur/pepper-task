import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  welcomeWrapper: {
    alignItems: 'center',
  },
  balanceWrapper: {
    alignItems: 'center',
  },
  welcomeText:{
    fontSize:30,
    fontWeight:'bold'
  },
  buttonWrapper:{
    alignItems: 'center',
  }
});
